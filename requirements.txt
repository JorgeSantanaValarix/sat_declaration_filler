# SAT Declaration Filler — Python 3.10+
# Install: pip install -r requirements.txt
# Then: playwright install chromium

openpyxl>=3.1.0
playwright>=1.40.0
pyodbc>=5.0.0
requests>=2.28.0
# Optional: evaluate Excel formulas when cached value is missing (no Excel needed)
xlcalculator>=0.5.0
